{
  "name": "littlelight-translatedtext-finder",
  "version": "1.0.0",
  "description": "A tool to find strings that needs translation on little light",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "João Paulo Marquesini",
  "license": "ISC",
  "dependencies": {
    "find-in-files": "^0.5.0",
    "fs-extra": "^7.0.1"
  }
}
